## Copy to .env (do not commit .env)
#
# Windows PowerShell:
#   Copy-Item .env.example .env
#
# Then fill in values.

# --- OpenAI (optional; required for AI explanation + chat) ---
OPENAI_API_KEY=

# --- Postgres (used by ETL scripts + dashboard) ---
DB_HOST=localhost
DB_PORT=5432
DB_NAME=donorcrm_db
DB_USER=postgres
DB_PASSWORD=

